node_modules
dist
out
release
.DS_Store
*.log
.env
.env.local
.env.production
.env.*
# 값이 없는 견본은 커밋한다 — 어떤 키가 필요한지 아는 유일한 곳이다 (BRU-71)
!.env.*.example
supabase/.env

# MCP config (contains tokens)
.mcp.json

# Playwright MCP가 남기는 스크린샷·스냅샷 (BRU-71 검증용, 산출물이다)
.playwright-mcp

# Serena MCP plugin
.serena/

# iOS (apps/ios)
# project.yml이 SoT다 — .xcodeproj는 `make ios-generate`로 만든다 (머지 충돌 회피)
apps/ios/Drop.xcodeproj/
xcuserdata/
DerivedData/
.build/
*.xcworkspace/xcshareddata/swiftpm/Package.resolved
.swiftpm/

# iOS 빌드 구성 (실제 값 — .example만 커밋)
apps/ios/Config/Config-*.xcconfig
!apps/ios/Config/*.example

# Android (apps/android)
apps/android/.gradle/
apps/android/build/
apps/android/*/build/
# SDK 경로와 Supabase 구성값이 들어간다 — 커밋 금지 (make android-config가 만든다)
apps/android/local.properties
apps/android/.kotlin/
