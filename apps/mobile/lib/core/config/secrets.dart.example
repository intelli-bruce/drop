/// Secrets configuration
/// Values are injected via --dart-define during build
/// For local development, you can override the default values
class Secrets {
  /// OpenAI API key for Whisper transcription
  /// - CI builds: Empty (compile-only, no actual API calls)
  /// - Release builds: Injected via --dart-define=OPENAI_API_KEY=xxx
  /// - Local dev: Set via --dart-define or modify this file (don't commit!)
  static const String openAIAPIKey = String.fromEnvironment(
    'OPENAI_API_KEY',
    defaultValue: '',
  );
}
